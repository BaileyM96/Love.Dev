import Card from "../components/Card";
// The LoginForm might be the problem its not defined anywhere in the card component
import Login from "../components/LoginForm";


export default function Discover() {
    return (
        <Card></Card>
    );
}